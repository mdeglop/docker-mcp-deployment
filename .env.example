# Environment Configuration for Docker MCP Gateway + n8n
# Copy this file to .env and fill in your values

# ============================================================================
# DOMAIN CONFIGURATION
# ============================================================================
# Your domain name (without https://)
# Example: mcp.yourdomain.com
DOMAIN=your-domain.com

# ============================================================================
# N8N CONFIGURATION
# ============================================================================
# n8n Basic Auth (for web UI access)
N8N_USER=admin
N8N_PASSWORD=changeme-strong-password-here

# n8n API Key (generate in n8n: Settings → API → Create API Key)
# This allows n8n-MCP to manage workflows
N8N_API_KEY=n8n_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# N8N-MCP AUTHENTICATION
# ============================================================================
# Generate with: openssl rand -hex 32
# IMPORTANT: Both AUTH_TOKEN and MCP_AUTH_TOKEN must be the SAME value
N8N_MCP_AUTH_TOKEN=generate-this-with-openssl-rand-hex-32

# ============================================================================
# OPTIONAL: SUPABASE CONFIGURATION
# ============================================================================
# If you're using Supabase MCP tools
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================================================
# OPTIONAL: GOOGLE SHEETS
# ============================================================================
# For Google Sheets access via MCP tools (if you add this to catalog)
# GOOGLE_SHEETS_API_KEY=your-api-key
# GOOGLE_SHEETS_CREDENTIALS=/path/to/credentials.json

# ============================================================================
# OPTIONAL: GITHUB MCP
# ============================================================================
# GitHub OAuth (handled by Docker MCP Gateway automatically)
# Or use personal access token:
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# OPTIONAL: BRAVE SEARCH
# ============================================================================
# For Brave Search MCP server
# BRAVE_API_KEY=your-brave-api-key

# ============================================================================
# ADVANCED: RESOURCE LIMITS (adjust if needed)
# ============================================================================
# Uncomment and modify if you need to adjust container resources
# MCP_GATEWAY_CPU_LIMIT=1.5
# MCP_GATEWAY_MEM_LIMIT=2g
# N8N_CPU_LIMIT=1.5
# N8N_MEM_LIMIT=3g

# ============================================================================
# NOTES
# ============================================================================
# 1. Never commit .env file to version control!
# 2. Keep your tokens secure
# 3. Generate strong passwords (use: openssl rand -base64 32)
# 4. For production, rotate tokens regularly
# 5. Use different tokens for development and production
